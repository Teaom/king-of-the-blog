{{!-- <style>
    /* Center the elements within the nav */
    nav {
        display: flex;
        justify-content: center;
    }

    /* Center the form and its child elements */
    #new-post-form {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
    }
</style>

<nav>
        <a href='/'>Home </a>
        <a href='/dashboard'>Dashboard </a>
        {{#if logged_in}}
        <a href='/'>Logout</a>
        {{/if}}
        {{#unless logged_in}}
        <a href="/login">Login</a>
        {{/unless}}
    </nav> --}}

<form id="new-post-form">
    <h3>Make a Post</h3>
    <h4>Title:</h4>
    <input type="text" id="title-post">
    <h4>Body:</h4>
    <textarea id="body-comment" style="height: 100px; width: 200px; resize: none;"></textarea>
    <button type="submit">Submit</button>
</form>

<script src="/js/newPost.js"></script>